// import provinces from "../../data/provinces.json";
// import districts from "../../data/districts.json";
// import subDistricts from "../../data/subDistricts.json";

// const getAddressWithLabels = (addressWithIds) => {
//   const { provinceId, districtId, subDistrictId } = addressWithIds;
//   const provinceLabel =
//     provinces.find((p) => p.id === provinceId)?.name || "N/A";

//   const districtLabel =
//     districts.find((d) => d.id === districtId)?.name || "N/A";

//   const subDistrictLabel =
//     subDistricts.find((s) => s.id === subDistrictId)?.name || "N/A";

//   return {
//     ...addressWithIds,
//     provinceLabel,
//     districtLabel,
//     subDistrictLabel,
//   };
// };

// export { getAddressWithLabels };
